VITE_CHECKLIST_API_URL=http://localhost:8081

# Pagamento (PIX / Mercado Pago)
# Base do backend de pagamento (cai para VITE_CHECKLIST_API_URL se vazio)
VITE_PAYMENT_API_URL=
# Dispensar pagamento (deixa passar direto). Default: ligado em dev, desligado em prod.
# VITE_PAYMENT_BYPASS=true
# Modo mock: gera um QR de exemplo sem backend (para visualizar a tela)
# VITE_PAYMENT_MOCK=true
